functions:
  oscar:
  - grnet:
      name: run-script-event2
      memory: 1Gi
      cpu: '1.0'
      image: opensourcefoundries/minideb:jessie
      script: script.sh
      environment:
        variables:
          MOUNT_PATH: "/mnt/run-script-event2/mount"
      input:
      - storage_provider: minio.default
        path: run-script-event2/in
      output:
      - storage_provider: minio.default
        path: run-script-event2/out
      mount:
        storage_provider: minio.default
        path: /run-script-event2/mount